运行方式：
ctrace 任意数字，数字须大于0。

代码中存在一个错误，1217行对n进行赋值（%n不向printf传递格式化信息，而是令printf把自己到该点已打出的字符总数放到相应变元指向的整形变量中）。n被赋值为48。WSPACELEN(n)的结果为-8，导致1218行和1238行中t->wspace[WSPACELEN(n)]发生了数组越界，实际访问的是t->level。1218行将t->level赋值为0，1238行将t->level赋值为32。导致bar函数从此无法再打印出任何信息。